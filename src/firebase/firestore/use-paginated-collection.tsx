'use client';
// Stub — Firestore paginated collection hook removed.
export function usePaginatedCollection(_query: any, _pageSize?: number) {
  return { data: null as any[] | null, isLoading: false, error: null, loadMore: () => {}, hasMore: false };
}
