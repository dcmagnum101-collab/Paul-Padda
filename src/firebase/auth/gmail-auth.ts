// Stub — Firebase auth removed. Gmail OAuth handled via next-auth.
export async function connectGmailAccount() {
  console.warn('connectGmailAccount: Firebase removed. Configure Gmail via next-auth.');
}
